<div class="pages">
  <h1>Results</h1>

  <% if @packaging == nil || @packaging == "" %>
    <h3>Volume: <%=('%.0f' % @calculated_volume)%> <%=@unit%></h3>
  <% else %>
    <h3>Packaging: <%=Constants::DISPLAY_PACKAGING[@packaging]%></h3>
  <% end %>

  <h3>ABV: <%=('%.1f' % @abv)%>%</h3>
  <h3>Number of Servings: <%=@number_of_servings%></h3>

  <% if @cost != 0 %>
    <h3>Cost: $<%=('%.2f' % @cost)%></h3>
    <h3>Cost per Serving: $<%=('%.2f' % @cost_per_serving)%></h3>
    <h3>Cost as a standard 750ml bottle of wine (12%ABV): $ <%=('%.2f' % @wine_cost)%></h3>
    <h3>Cost as a standard 750ml bottle of spirts (80 Proof): $ <%=('%.2f' % @spirits_cost)%></h3>
    <h3>Cost as a 6pk-12oz bottles/cans of beer (5%ABV): $ <%=('%.2f' % @beer_6pk_cost)%></h3>
    <h3>Cost as a 4pk-16oz bottles/cans of beer (5%ABV): $<%=('%.2f' % @beer_4pk_cost)%></h3>
  <% end %>

  <button class="button" type="submit"><%= link_to 'Bartender!  Another!', new_home_path %></button>
</div>
